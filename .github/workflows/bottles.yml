name: Build & Upload Bottles

on:
  push:
    branches: ["**"]
    paths: ["Formula/*.rb"]
  workflow_dispatch: {}

permissions:
  contents: write
  pull-requests: write

jobs:
  bottle_and_publish:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: Homebrew/actions/setup-homebrew@master
      - name: Build bottle(s)
        env:
          HOMEBREW_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            FORMULAE="$(find Formula -name '*.rb' -exec basename {} .rb \; | sort | tr '\n' ' ')"
          else
            FORMULAE="$(git diff --name-only "${{ github.event.before }}" "${{ github.sha }}" -- 'Formula/*.rb' \
              | sed -E 's#^Formula/##; s#\.rb$##' \
              | sort -u \
              | tr '\n' ' ')"
          fi
          [ -n "$FORMULAE" ] || { echo "No formula changes detected"; exit 0; }
          echo "Building bottles for: $FORMULAE"
          brew test-bot --only-formulae --skip-online-checks \
            --tap="${{ github.repository }}" \
            --testing-formulae $FORMULAE
      - name: Update formula with bottle and push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HOMEBREW_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          TAP="${{ github.repository }}"
          TAP_DIR="$(brew --repository "$TAP")"
          have_files=0
          ls -1 *.bottle*.json *.bottle*.tar.gz >/dev/null 2>&1 && have_files=1 || true
          if [ $have_files -eq 0 ]; then
            ls -1 "$TAP_DIR"/*.bottle*.json "$TAP_DIR"/*.bottle*.tar.gz >/dev/null 2>&1 && have_files=1 || true
          fi
          [ $have_files -eq 1 ] || { echo "No bottle files found"; exit 1; }
          cd "$TAP_DIR"
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git"
          brew pr-upload
          branch="${GITHUB_HEAD_REF:-${GITHUB_REF_NAME}}"
          git push origin HEAD:"$branch"
